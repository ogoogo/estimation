clear()
distances = 10000*[7,10,15,20,25,30,40,50,60,70,80,90,100];
min_deg = 0;
max_deg = 45;
n=30;

for i = 1:4
    for j = 1:12
        build1(distances(j),max_distance, min_deg, max_deg,n)
        min_distance = max_distance;
        max_distance = max_distance + 100000;
        disp(min_distance)
        disp(max_distance)
        disp(min_deg)
        disp(max_deg)

    end
    min_deg = max_deg;
    max_deg = max_deg + 45;
    min_distance = 70000;
    max_distance = 100000;
end